from setuptools import setup, find_packages

setup(
    name='syn_cerebral_octa_seg',
    version='0.1',
    description='syn_cerebral_octa_seg',
    packages=find_packages(),
    entry_points={},
    python_requires=">=3.8"
)